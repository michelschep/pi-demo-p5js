// pi-demo-p5js — main sketch
// Logic lives in src/ modules, this file stays thin

function setup() {
  createCanvas(800, 600);
}

function draw() {
  background(15, 17, 23);
  // Features will be added by the Wiggum pipeline
}
